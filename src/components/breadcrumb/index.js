import Breadcrumb from './Breadcrumb.vue';
import BreadcrumbItem from './BreadcrumbItem.vue';

export default function (Vue) {
  Vue.component('ou-breadcrumb', Breadcrumb);
  Vue.component('ou-breadcrumb-item', BreadcrumbItem);
}
